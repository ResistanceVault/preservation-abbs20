/* This file contains empty template routines that
 * the IDCMP handler will call uppon. Fill out these
 * routines with your code or use them as a reference
 * to create your program.
 */

int HoldpathClicked( void )
{
	/* routine when gadget "Hold Path" is clicked. */
}

int tmppathClicked( void )
{
	/* routine when gadget "Tmpdir Path" is clicked. */
}

int Window_xClicked( void )
{
	/* routine when gadget "Window x-pos" is clicked. */
}

int Window_yClicked( void )
{
	/* routine when gadget "Window y-pos" is clicked. */
}

int Window_heightClicked( void )
{
	/* routine when gadget "Height" is clicked. */
}

int Window_widthClicked( void )
{
	/* routine when gadget "Width" is clicked. */
}

int NodetypeClicked( void )
{
	/* routine when gadget "" is clicked. */
}

int Modem_InitClicked( void )
{
	/* routine when gadget "Modem Init" is clicked. */
}

int Modem_on_hookClicked( void )
{
	/* routine when gadget "Modem On Hook" is clicked. */
}

int Modem_off_hookClicked( void )
{
	/* routine when gadget "Modem Off Hook" is clicked. */
}

int Modem_answerClicked( void )
{
	/* routine when gadget "Modem Answer" is clicked. */
}

int Modem_dialClicked( void )
{
	/* routine when gadget "Modem Dial" is clicked. */
}

int Modem_ringClicked( void )
{
	/* routine when gadget "Modem Ring" is clicked. */
}

int Modem_connectClicked( void )
{
	/* routine when gadget "Modem Connect" is clicked. */
}

int Modem_no_carrierClicked( void )
{
	/* routine when gadget "Modem No Carrier" is clicked. */
}

int Modem_at_stringClicked( void )
{
	/* routine when gadget "Modem AT String" is clicked. */
}

int Minimum_baudClicked( void )
{
	/* routine when gadget "Minimum Baud" is clicked. */
}

int Connect_waitClicked( void )
{
	/* routine when gadget "Connect Wait" is clicked. */
}

int Modem_machine_baudClicked( void )
{
	/* routine when gadget "Modem-machine baud" is clicked. */
}

int Min_between_loginClicked( void )
{
	/* routine when gadget "Set Min Time Between two logins" is clicked. */
}

int Max_login_timeClicked( void )
{
	/* routine when gadget "Set Max Login Time" is clicked. */
}

int Locked_baud_rateClicked( void )
{
	/* routine when gadget "Locked Baud Rate" is clicked. */
}

int CTS_RTSClicked( void )
{
	/* routine when gadget "CTS/RTS" is clicked. */
}

int Hangup_modeClicked( void )
{
	/* routine when gadget "+++ Hangup" is clicked. */
}

int Comm_port_nameClicked( void )
{
	/* routine when gadget "Comm port name" is clicked. */
}

int Comm_portClicked( void )
{
	/* routine when gadget "Comm Port" is clicked. */
}

int Tiny_modeClicked( void )
{
	/* routine when gadget "Open at startup" is clicked. */
}

int screenmodeClicked( void )
{
	/* routine when gadget "" is clicked. */
}

int publicscrenClicked( void )
{
	/* routine when gadget "" is clicked. */
}

int node_typeClicked( void )
{
	/* routine when gadget "" is clicked. */
}

int get_fontClicked( void )
{
	/* routine when gadget "" is clicked. */
}

int get_hold_pathClicked( void )
{
	/* routine when gadget "" is clicked. */
}

int get_tmpdir_pathClicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m20Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m21Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m22Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m23Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m24Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m1Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m2Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m3Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m4Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m5Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m6Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m7Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m8Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m9Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m10Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m11Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m12Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m13Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m14Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m15Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m16Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m17Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m18Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int m19Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int Max_time_read_gadgetsClicked( void )
{
	/* routine when gadget "OK" is clicked. */
}

int b20Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b21Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b22Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b23Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b24Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b1Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b2Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b3Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b4Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b5Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b6Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b7Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b8Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b9Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b10Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b11Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b12Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b13Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b14Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b15Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b16Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b17Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b18Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int b19Clicked( void )
{
	/* routine when gadget "" is clicked. */
}

int Min_time_read_gadgetsClicked( void )
{
	/* routine when gadget "OK" is clicked. */
}

int ConfignodeAbout( void )
{
	/* routine when (sub)item "About" is selected. */
}

int ConfignodeLoad_config( void )
{
	/* routine when (sub)item "Load Config" is selected. */
}

int ConfignodeSave_config( void )
{
	/* routine when (sub)item "Save Config" is selected. */
}

int ConfignodeExit_main( void )
{
	/* routine when (sub)item "Quit" is selected. */
}

int ConfignodeCloseWindow( void )
{
	/* routine for "IDCMP_CLOSEWINDOW". */
}

int Max_loginCloseWindow( void )
{
	/* routine for "IDCMP_CLOSEWINDOW". */
}

int Time_betweenCloseWindow( void )
{
	/* routine for "IDCMP_CLOSEWINDOW". */
}

